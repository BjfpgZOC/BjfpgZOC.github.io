source "https://rubygems.org"

# Use GitHub Pages gem for syncing with GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Use the latest unreleased version of Minimal Mistakes
gem "minimal-mistakes-jekyll", :github => "mmistakes/minimal-mistakes"

# The following plugins are automatically loaded by the GitHub Pages gem.
# Listing them here ensures they work in local builds too.
group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-gist"
  gem "jekyll-feed"
  gem "jekyll-include-cache"
end